<Window x:Class="SlipperIS.UI.Views.StockReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="库存报表" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Background="White" Margin="8,8,8,4" CornerRadius="4">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                <TextBlock Text="类别：" VerticalAlignment="Center" Margin="0,0,4,0" Foreground="#555"/>
                <ComboBox x:Name="CmbCategory" Width="120" Height="28" BorderBrush="#CCC" BorderThickness="1"
                          SelectionChanged="Category_SelectionChanged">
                    <ComboBoxItem Content="全部" IsSelected="True"/>
                </ComboBox>
                <Button Content="刷新" Margin="8,0,0,0" Padding="12,4"
                        Background="#2196F3" Foreground="White" BorderThickness="0"
                        Cursor="Hand" Click="Refresh_Click"/>
            </StackPanel>
        </Border>

        <!-- 汇总统计 -->
        <Grid Grid.Row="1" Margin="8,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#2196F3" CornerRadius="4" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Text="产品种类" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock x:Name="TxtProductCount" Text="0" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    <TextBlock Text="种" Foreground="White" FontSize="11" Opacity="0.8"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Background="#4CAF50" CornerRadius="4" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Text="库存总量" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock x:Name="TxtTotalQty" Text="0" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    <TextBlock Text="件" Foreground="White" FontSize="11" Opacity="0.8"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Background="#FF9800" CornerRadius="4" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Text="库存总价值" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock x:Name="TxtTotalValue" Text="¥0.00" Foreground="White" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="3" Background="#F44336" CornerRadius="4" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Text="库存预警" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock x:Name="TxtLowStockCount" Text="0" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    <TextBlock Text="种不足" Foreground="White" FontSize="11" Opacity="0.8"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 库存列表 -->
        <DataGrid x:Name="ReportGrid" Grid.Row="2"
                  Margin="8,4" AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="True"
                  SelectionMode="Single" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" AlternatingRowBackground="#FAFAFA"
                  BorderBrush="#DDD" BorderThickness="1">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                            <Setter Property="Background" Value="#FFEBEE"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="产品编码" Binding="{Binding ProductCode}" Width="100"/>
                <DataGridTextColumn Header="产品名称" Binding="{Binding ProductName}" Width="160"/>
                <DataGridTextColumn Header="类别" Binding="{Binding Category}" Width="80"/>
                <DataGridTextColumn Header="当前库存" Binding="{Binding StockQuantity}" Width="90"/>
                <DataGridTextColumn Header="最低库存" Binding="{Binding MinStockLevel}" Width="90"/>
                <DataGridTextColumn Header="成本价" Binding="{Binding CostPrice, StringFormat=\{0:F2\}}" Width="80"/>
                <DataGridTextColumn Header="销售价" Binding="{Binding SalesPrice, StringFormat=\{0:F2\}}" Width="80"/>
                <DataGridTextColumn Header="库存价值" Binding="{Binding StockValue, StringFormat=\{0:F2\}}" Width="100"/>
                <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 底部栏 -->
        <Border Grid.Row="3" Background="White" Margin="8,4,8,8" Padding="8,0" CornerRadius="4">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="#666" FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
